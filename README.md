# Campus Recruitment PM Assistant V1

上传不同项目的 Excel 排期表，统一导入任务数据库，并生成今日待办、3 天内截止、逾期和本周任务。

## 启动

双击 `run.bat`，或运行：

```powershell
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

